import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pci.champhai.bethel',
  appName: 'pci-champhai-bethel-kohhran',
  webDir: 'dist'
};

export default config;
