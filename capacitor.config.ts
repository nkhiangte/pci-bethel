import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.pcibethel.app',
  appName: 'Champhai Bethel Kohhran',
  webDir: 'dist'
};

export default config;
